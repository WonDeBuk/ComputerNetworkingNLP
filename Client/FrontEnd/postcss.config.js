// Export the configuration object for PostCSS
export default {
    // Define the plugins to use
    plugins: {
        // Enable the @tailwindcss/postcss plugin for Tailwind CSS v4
        "@tailwindcss/postcss": {},
        // Enable the autoprefixer plugin to add vendor prefixes
        autoprefixer: {},
    },
}
