// Import the ConnectionPage component
import ConnectionPage from './components/ConnectionPage';

// Define the main App component
function App() {
  // Render the ConnectionPage component
  return (
    // Use a fragment or div to wrap the content
    <div>
      <ConnectionPage />
    </div>
  );
}

// Export the App component as default
export default App;
