# General
.DS_Store
Thumbs.db
*.log

# Editors
.vscode/
.idea/
*.swp
*.swo

# C++ Server
Server/Debug/
Server/Release/
Server/x64/
*.exe
*.obj
*.o
*.pdb
*.ilk
*.idb
*.pch
*.user
*.suo
*.db

# Node.js (Gateway & FrontEnd)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
# Keep package-lock.json if you want to lock versions, but often ignored in learning projects. 
# Standard practice is to KEEP it, but I will leave it not ignored by default usually. 
# Let's actually NOT ignore package-lock.json as it is good practice to commit it.
# So I will remove package-lock.json from ignore list.

# FrontEnd Build
Client/FrontEnd/dist/
Client/FrontEnd/build/
.env.local
.env.development.local
.env.test.local
.env.production.local

# Gateway
Client/Gateway/.env
